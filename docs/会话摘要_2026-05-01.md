# 工作日志 2026-05-01

## 今日完成

### 1. Agent 系统修复并测试通过
**问题修复**：
- **MiniMax API "chat content is empty"** - 所有 `llm.invoke()` 调用添加 `HumanMessage` 作为 second message（MiniMax 要求必须有两个 message）
- **JSON 解析错误** - `heuristic_teaching_node` 中 `tool_params` JSON 解析加了 try-except 保护
- **无限循环** - 优化 `_rule_based_route` 规则，增加"刚执行完 heuristic_teaching 后直接进 response"的规则
- **消息丢失** - `chat()` 接口现在正确追踪 response_node 的状态（包含 AI 消息的那个），而不是最后的 supervisor 状态
- **END 路由问题** - `_route_edge` 返回 `"__end__"` 字符串与字典映射对应

**执行流程**：
```
supervisor → learning_diagnosis → emotion_detection → heuristic_teaching → response
```

**测试结果**：输入"Why is f(x)=x^2 even?"，输出1184字完整回复，约56秒

### 2. 智能辅导页面开发
**页面布局**：
- **左侧工作区（60%）**：
  - 顶部：学生问题卡片
  - 中间：AI解题空间，分5步展示（概念→定理→公式→计算→解释）
  - 右下角悬浮按钮：📐GeoGebra画板、🔍知识点溯源
- **右侧对话区（40%）**：
  - AI导师头像 + 文字/语音模式切换
  - 苏格拉底式引导对话（AI灰左、用户蓝右）
  - 底部多模态输入（📷拍照、🖼️上传、🎤语音、文本输入框）

**弹窗功能**：
- 📐 GeoGebra 弹窗 - 工具选择（函数图像/几何图形/Venn图/数轴区间）
- 🔍 知识点溯源弹窗 - 知识路径图 + 解题路径总结

## 待推进（后续继续）

### 1. 智能辅导页 解题空间动态化
- 当前是预设全部步骤，后续根据学生响应逐步展示
- 不同题目类型有不同的步骤组合（有的有概念，有的直接计算）
- 根据 APOS 学情诊断状态来控制显示逻辑

### 2. RAG 向量化升级
- 当前使用 TF-IDF
- 后续升级到 Qdrant + BGE-M3 嵌入模型

### 3. 知识图谱
- 当前 CSV 格式
- 后续迁移到 Neo4j 图数据库

### 4. 学情追踪Agent
- 记录历史答题
- 调整 APOS 判断
- 推荐学习路径

### 5. 练习生成Agent / 错题分析Agent

## 技术记录

### MiniMax API 调用注意
- 必须传两个 message（SystemMessage + HumanMessage），否则报 "chat content is empty" 错误

### LangGraph StateGraph 路由
- `_route_edge` 返回字符串 `"__end__"` 与条件边字典的 `__end__` key 对应
- 状态更新：节点返回的 dict 会合并到 state
- 但 `app.stream(state)` 返回的 event 是 `{"节点名": updated_state}`