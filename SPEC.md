# 数学AI智能体教学平台 - 技术规格文档

> 本文档是项目的核心规格文档，记录技术决策、架构设计、开发规范。
> 随着项目推进持续更新。最后更新：2026-04-27

---

## 一、项目概述

### 1.1 项目背景
- **阶段**：概念验证（0→1）
- **目标**：面向中学数学教学的AI智能体，支持学生个性化答疑、教师课件生成、多智能体协同虚拟课堂
- **当前优先级**：MVP Demo for Sponsor，聚焦学生个性化答疑场景

### 1.2 核心功能范围
| 功能 | MVP是否实现 | 说明 |
|------|-------------|------|
| 学生个性化答疑 | ✅ MVP核心 | 自动化，APOS自适应 |
| 多模态学习资源生成 | ✅ MVP核心 | GeoGebra动态图形 + 语音讲解 |
| 教师课件生成 | ❌ 后续迭代 | - |
| 多智能体协同课堂 | ❌ 后续迭代 | - |
| 学情分析 | ⚠️ 简化版 | MVP仅做APOS阶段推断 |
| 家长/教师监控 | ❌ MVP跳过 | - |

### 1.3 目标用户
- **主要用户**：高一学生
- **科目**：高中数学（第一阶段聚焦人教版必修一）

---

## 二、技术架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      应用交互层                              │
│              (MVP: 学生端对话界面 + GeoGebra)               │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                    引导策略层 (APOS)                         │
│         苏格拉底式Prompt模板 + APOS阶段判断                   │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                    Agent Orchestrator                        │
│              (LangGraph ReAct 循环编排)                      │
│   问题理解 → APOS推断 → 策略选择 → 资源生成                   │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                      知识检索层 (RAG)                        │
│              向量检索(Qdrant) + 提示词模板                    │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                      数据资源层                              │
│         预处理知识点文档(Markdown + Latex)                    │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 技术栈

| 组件 | 选型 | 版本/说明 |
|------|------|-----------|
| **LLM基座** | Claude 3.5 Sonnet / GPT-4o | 数学推理能力强 |
| **Agent框架** | LangGraph | ReAct范式，支持状态机编排 |
| **RAG引擎** | Qdrant | 向量检索，轻量部署 |
| **向量模型** | BGE-M3 | 中文支持好 |
| **前端框架** | Streamlit | 快速原型，展示效果好 |
| **动态几何** | GeoGebra Classic | 业界最成熟，Web嵌入 |
| **语音合成** | Azure TTS / CosyVoice | 效果自然（可选后期集成）|
| **数学计算** | SymPy | 符号计算，生成Latex |
| **公式渲染** | KaTeX / MathJax | 前端数学公式渲染 |

### 2.3 Agent设计

#### 2.3.1 多Agent协作架构（Phase 1 单实例）

```
┌─────────────────────────────────────────────────────────────┐
│                    Global State (LangGraph)                  │
│              学情 / 情绪 / 历史消息 / 工具调用               │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                 Supervisor（调度中心）                       │
│        职责：状态读取 + 路由决策 + next_node写入            │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│  学情诊断Agent │   │ 启发讲授Agent │   │ 问题解决Agent │
│  APOS判断     │   │ 苏格拉底提问  │   │ ReAct推理     │
│  学习路径     │   │ ZPD内支架问题 │   │ 逻辑启发      │
└───────────────┘   └───────────────┘   └───────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              ▼
                  ┌───────────────────────┐
                  │ 虚拟学习伴侣Agent      │
                  │ 常驻监听 + 情绪检测    │
                  │ 动机调节               │
                  └───────────────────────┘
                              │
                              ▼
                  ┌───────────────────────┐
                  │    工具执行层          │
                  │ render_graph/animate   │
                  │ show_card/derivation   │
                  └───────────────────────┘
```

#### 2.3.2 四个 Agent 职责

| Agent | 核心职责 | 关键能力 |
|-------|---------|---------|
| **学情诊断 Agent** | 判断学生 APOS 阶段 | 决定用哪种交互方式 |
| **问题解决 Agent** | ReAct 逻辑推理 | 启发式引导，不直接给答案 |
| **启发讲授 Agent** | 苏格拉底式提问 | 生成 ZPD 内的支架问题，调用画板/公式组件 |
| **虚拟学习伴侣 Agent** | 情绪识别 + 动机调节 | 常驻监听，跨所有 Agent 输出 |

#### 2.3.3 Supervisor 定位

- **职责**：状态读取 + 路由决策 + 状态更新
- **不是**超级智能体，只做路由判断
- 通过 `next_node` 字段控制下一个执行的节点

#### 2.3.4 Supervisor 路由规则：A+B 混合

**核心思路**：先用规则决策（B），规则无法决策时调用 LLM（A）

**B 规则（规则决策）**：

| 规则 | 条件 | 路由 |
|------|------|------|
| 规则 1 | `apos_level` 为空 | → `learning_diagnosis` |
| 规则 2 | `pending_tool` 存在 | → `tool_execution` |
| 规则 3 | `consecutive_errors >= 3` | → `emotion_detection` |
| 规则 4 | `messages[-1].role == "user"` | → `emotion_detection` |
| 规则 5 | 情绪检测后 | 见下方 |

**情绪检测后路由**：

| 情绪 | 动机 | 路由 |
|------|------|------|
| frustrated | < 0.4 | → `heuristic_teaching`（降难度）|
| confident / excited | 任意 | → `problem_solving`（趁热打铁）|
| 其他 | 任意 | → `heuristic_teaching` |

**A 规则（LLM 决策）**：
- 规则无法决策时（返回 `UNDECIDED`）→ 调用 LLM 判断下一步

#### 2.3.5 虚拟学习伴侣 Agent 设计

- **常驻监听 + 条件增强**，不单独作为打断学生的一个节点
- 在其他 Agent 输出时做情绪检测，影响输出语气
- 检测到挫败感 → 后续输出自动切换到鼓励口吻
- 检测到无聊 → 插入趣味提问

#### 2.3.6 APOS状态机设计

**状态定义**：

| 阶段 | 业务定义 | 升阶触发 | 降阶触发 | 降阶后教学 |
|------|---------|---------|---------|-----------|
| **Action** | 初始态，需借助具体数字/图表思考 | 内化 (Interiorization) | — | GeoGebra 画板 |
| **Process** | 内化操作流程，想象动态映射关系 | 封装 (Encapsulation) | 认知阻滞 (cognitive_block) | 拆解过程分析 |
| **Object** | 将过程打包成名词/客体 | 主题化 (Thematization) | 去封装化 (de_encapsulation) | 退 Process 分析 |
| **Schema** | 知识网络建构完成，准许流转 | ✅ 达成 | **不降阶** | 复习激活 |

**升阶跃迁**：

```
Action ──[内化]──→ Process ──[封装]──→ Object ──[主题化]──→ Schema
   ↑                  │               │
   │                  │               │
   └─[认知阻滞]───────┘──[去封装化]───┘
```

**检测函数**：

| 转移 | 检测方式 | 正面指标 | 负面指标 |
|------|---------|---------|---------|
| Action→Process | 能描述连续规律，不用具体数字 | "增大"、"减小"、"随x变化" | "x=1"、"代入" |
| Process→Object | 使用抽象名词 | "增函数"、"偶函数" | 描述过程 |
| Object→Schema | 关联其他知识点 | "类似"、"联系"、"方程" | — |

**主观题评估**：

- 关键概念覆盖率 ≥ 70% 视为通过
- 用于 Process/Object 阶段的理解判断

**APOS 追踪数据结构**：

```python
apos_tracking: dict[str, dict] = {
    "奇偶性": {
        "level": "action",           # 当前阶段
        "consecutive_correct": 0,     # 连续正确次数
        "consecutive_errors": 0,      # 连续错误次数
        "last_practice": timestamp,   # 上次练习
        "evidence": [],              # 证据记录
        "mastered_at": timestamp,     # Schema 达成时间
    }
}
```

**证据记录格式**：

```python
{
    "timestamp": time.time(),
    "type": "encapsulation",       # 升阶类型
    "from_level": "process",
    "to_level": "object",
    "confidence": 0.8,
    "description": "学生使用了抽象名词：['增函数']"
}
```

#### 2.3.7 ReAct执行流程

```
Thought → Action → Observation → Thought → ... → Final Answer
```

### 2.4 多模态输出形式

| 模态 | 内容 | 技术实现 |
|------|------|----------|
| 文本 | 启发式引导 + 题解步骤 | LLM直接生成 |
| 动态图形 | 几何/函数可视化 | GeoGebra嵌入 |
| 语音 | 口语化讲解 | TTS API（后期）|
| 公式渲染 | 数学公式显示 | KaTeX/MathJax |

---

## 三、知识库设计

### 3.1 知识库实际结构

```
knowledge/
├── 第一章_集合与常用逻辑用语/
│   ├── 1.1_集合的概念.md          ✅ 已创建
│   ├── 1.2_集合间的基本关系.md    ✅ 已创建
│   ├── 1.3_集合的基本运算.md       ✅ 已创建（含GeoGebra配置）
│   ├── 1.4_充分条件与必要条件.md  ✅ 已创建
│   └── 1.5_全称量词与存在量词.md  ✅ 已创建
├── knowledge_graph/                ✅ 已创建（数据表，MVP可不用）
│   ├── 知识点表.csv
│   ├── 知识点关系表.csv
│   ├── 题目表.csv
│   ├── APOS定义表.csv
│   └── 数据库Schema说明.md
├── README.md                       ✅ 已创建（结构说明）
└── prompts/                        🔲 待创建（Prompt模板）
```

### 3.2 APOS阶段定义

每个知识点包含四阶段定义：

```yaml
APOS阶段:
  Action（操作）: 学生能按步骤执行
  Process（过程）: 学生理解本质原理
  Object（对象）: 学生能把知识点当整体使用
  Schema（图式）: 学生能综合应用解决新问题
```

### 3.3 提示词模板结构

```
【知识背景】
{检索到的知识点定义}

【关键定理/公式】
{检索到的定理}

【启发式引导方向】
{根据APOS阶段选择的引导策略}

【苏格拉底式提问模板】
{预设的提问模板占位符}
```

### 3.4 数据表设计（支持后续扩展）

| 表名 | 用途 | MVP是否使用 |
|------|------|-------------|
| 知识点表 | 知识点元数据 | ⚠️ 可选 |
| 知识点关系表 | 知识点关联关系 | ⚠️ 可选 |
| 题目表 | 例题/习题库 | ⚠️ 可选 |
| APOS定义表 | APOS阶段标准定义 | ⚠️ 可选 |

**说明**：MVP阶段使用markdown文档+RAG即可，数据表用于后续扩展

详见 [数据库Schema说明](knowledge/knowledge_graph/数据库Schema说明.md)

### 3.5 Agent系统设计

详见 [Agent设计文档](docs/Agent设计文档.md)

内容包括：
- System Prompt（4个Agent角色定义）
- 数据结构（状态传递格式）
- 工作流（LangGraph流程）
- 工具定义（search_knowledge等）

---

## 四、开发规范

### 4.1 代码组织

```
edu_agent/
├── agents/                     # Agent 系统（Phase 1 单实例）
│   ├── __init__.py
│   ├── state.py               # StudentState 定义 + 初始状态
│   ├── prompts.py             # 各 Agent 的 Prompt 模板
│   ├── nodes.py               # 6 个节点实现
│   └── graph.py               # LangGraph StateGraph + 对话接口
├── config/
│   └── api_config.py          # API Key 配置
├── tools/                      # 工具层
├── knowledge/                  # 知识库
└── app.py                      # Streamlit主入口
```

**Phase 1 实现状态**：
- ✅ `agents/state.py` - StudentState 定义
- ✅ `agents/prompts.py` - Prompt 模板
- ✅ `agents/nodes.py` - 节点实现（草稿）
- ✅ `agents/graph.py` - StateGraph 框架
- 🔄 待完善：Prompt 长度优化、Agent 逻辑调优

### 4.2 Prompt编写规范

- 每个Agent有独立的system prompt
- Prompt模板存放在`knowledge/prompts/templates/`
- 苏格拉底式提问使用预设模板，不依赖LLM自由生成

### 4.3 知识库更新规范

- 新增知识点：创建独立md文件
- 更新知识点：修改对应md文件后重建向量索引
- APOS标注：每个知识点必须包含APOS四阶段定义

---

## 五、里程碑与计划

### 5.1 MVP开发阶段

| 阶段 | 时间 | 目标 |
|------|------|------|
| Phase 1 | Week 1-2 | 知识库搭建 + Agent骨架 |
| Phase 2 | Week 3-4 | 核心Agent逻辑（APOS判断 + 策略生成）|
| Phase 3 | Week 5-6 | 多模态集成（GeoGebra + TTS）|
| Phase 4 | Week 7-8 | 串联调试 + Demo准备 |

### 5.2 MVP验收标准

- [ ] 学生提问一道集合/函数/三角函数题目
- [ ] 系统能返回带GeoGebra动态图形的解答
- [ ] APOS阶段能自适应调整引导策略
- [ ] 能展示给sponsor并体现多模态效果

---

## 六、待决策事项

| 事项 | 状态 | 备注 |
|------|------|------|
| 教材电子版 | ✅ 已获取 | 人教版必修一第一章完整内容 |
| GeoGebra具体嵌入方案 | 🔄 待测试 | Web版 vs Embed API |
| TTS具体选型 | 🔄 待定 | Azure vs CosyVoice |
| 向量数据库部署方式 | 🔄 待定 | 云服务 vs 本地 |
| **RAG向量检索升级** | 🔄 待升级 | 当前TF-IDF，后续升级Qdrant+BGE-M3 |
| **智能辅导页面** | ✅ 已完成 | 左侧AI解题空间 + 右侧苏格拉底对话 |

## 六.2 后续升级计划

| 升级项 | 当前状态 | 目标 | 优先级 |
|--------|----------|------|--------|
| **RAG向量化** | TF-IDF | Qdrant + BGE-M3嵌入模型 | P1 |
| 知识图谱 | CSV | Neo4j图数据库 | P2 |
| **学情追踪Agent** | 无 | 记录历史答题、调整APOS判断、推荐学习路径 | P3 |
| 练习生成Agent | 无 | 根据APOS生成练习题 | P4 |
| 错题分析Agent | 无 | 分析学生错误原因 | P5 |
| **智能辅导解题空间动态化** | 预设静态步骤 | 根据学生响应+APOS状态逐步展示 | P6 |

详见 [Agent设计文档_扩展需求](docs/Agent设计文档_扩展需求.md)

---

## 七、参考资料

- APOS理论：Dubinsky, E. & Schappelle, M. (1994)
- ReAct范式：Yao et al. (2023) "ReACT: Synergizing Reasoning and Acting in Language Models"
- GeoGebra嵌入文档：https://www.geogebra.org/manual/en/Embedding
- LangGraph文档：https://langchain-ai.github.io/langgraph/

---

## 八、变更记录

| 日期 | 变更内容 | 变更人 |
|------|----------|--------|
| 2026-04-26 | 初始版本，基于项目讨论创建 | Claude |
| 2026-04-27 | 获取教材电子版，创建第一章知识库（1.1-1.3），完成APOS标注和GeoGebra配置 | Claude |
| 2026-05-01 | Agent系统测试通过；智能辅导页面完成 | Claude |
| 2026-05-01晚 | 多轮对话改进：修复Rule3避免误杀学情已诊断对话；response_node保留学情状态；learning_diagnosis_node支持微调掌握度；添加learning_history记录 | Claude |
